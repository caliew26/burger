{{!-- this is what the user will see and interact with --}}
<h1>Mouth watering B-U-R-G-E-R-S!!</h1>
<div class="wrapper">
  <div class="burgersNotEaten">
    <h3>These burgers are Available</h3>
    <ul>
      {{!-- this is a forloop,  --}}
      {{#each burger}}
        {{#unless this.devoured}}
          {{this.id}}.{{this.burger_name}}<br>
        {{/unless}}
      {{/each}}
    </ul>
  </div>

  <div class="eatenBurgers">
    <h3>The Burgers are GONE</h3>
    <ul>
      {{#each burger}}
        {{#if this.devoured}}
          {{this.id}}.{{this.burger_name}}<br>
        {{/if}}
      {{/each}}
    </ul>
  </div>

  {{!-- <div class="pictures">
      <img src="/assets/img/burgerbackground.jpg" alt="burger background">
  </div> --}}


  <form class="create-form">
    <div class="form-group">
      <label for="burgerX"><b>Wanna Add A Burger?</b></label><br>
      <input type="text" id="burgerY" name="name">
    </div>

    <div class="form-group">
      <label for="devour">Have you tried it?</label><br>
      <input type="radio" name="devoured" value="1"/> YES!<br>
      <input type="radio" name="devoured" value="0"/> NO!<br>
      <button class="newBurgerBtn" type="submit">Add Burger</button>
      {{!-- <input type="button" class="newBurgerBtn" value="Add Burger"/> --}}
    </div>
  </form>





</div>